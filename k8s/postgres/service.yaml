apiVersion: v1
kind: Service
metadata:
  name: postgres          # Other pods reach Postgres at host "postgres:5432"
  namespace: chatapp
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP          # Internal only — Postgres should never be exposed outside the cluster
