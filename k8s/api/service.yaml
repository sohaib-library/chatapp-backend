apiVersion: v1
kind: Service
metadata:
  name: chatapp-api
  namespace: chatapp
spec:
  selector:
    app: chatapp-api
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30800     # Access API at http://localhost:8000 (mapped by k3d --port flag)
  type: NodePort
